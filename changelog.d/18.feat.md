Add `to_drag_menu_command`
and `to_drag_double_click_command` arguments to `MayaMenuBuilder`.

If provided, `MayaMenuBuilder` will generate `dragMenuCommand`
and `dragDoubleClickCommand` dynamically when a menu item is added to the shelf.
