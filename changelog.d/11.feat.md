The `load` and `loads` functions accepts an optional `parser` argument.

The `parser` argument can be used to load configuration from JSON or YAML files.
If no `parser` is provided, the functions default to loading a TOML document,
for backward compatibility.
